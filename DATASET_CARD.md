# 数据集卡片：CEUA Benchmark

> CEUA Benchmark · 412 道题 · 206 个基础情境 · 中文


## 这个数据集是什么

**CEUA Benchmark** 是一个面向中文的情绪智力（Emotional Intelligence）评测集，共 **412 道题**。它考察语言模型的两件事：能不能看懂一段人际情境里当事人的情绪，以及能不能在这样的情境下给出得体、有支持性的回应或行动建议。

需要先指出一点：这 412 道题并不是 412 个互不相干的故事。它们其实来自 **206 个基础情境**——每个情境都会被用两次，一道题测理解，另一道测应用。所以真正的情境数量是 206.

| 模块 | 题目数 | 测什么 |
| --- | ---: | --- |
| EU | 206 | 情绪理解（Emotional Understanding） |
| EA | 206 | 情绪应用（Emotional Application） |
| 合计 | 412 | |


### EU：情绪理解

EU 给出一段中文情境描述，通常是某种复杂或微妙的人际处境，要求从 4 个选项里挑出一个情绪类别。

这里有个容易误会的地方：金标准指的是这段情境里**最可能出现的主导情绪**，而不是"当事人只可能有这一种情绪"。一个人同时有几种情绪本来就再正常不过，限定成一个标签，只是为了让模型的输出能够被一致地打分。

### EA：情绪应用

EA 同样从一个有情绪困难的情境出发，但问的是另一个问题：这种情况下，什么样的回应或行动才是合适的？

它的取向大致有两条：

- **回应（Respond）**：情绪上的确认、共情，让人感到被理解、被支持；
- **行动（Action）**：提供恰当的帮助、建设性的建议，或指出可以求助的资源。

EA 的金标准是**人工斟酌后给出的规范性判断**，不是"放之四海而皆准的唯一答案"。这一点直接影响 EA 分数的解读方式，后面还会再展开。

## EU 和 EA 是什么关系

每个基础情境都会被用两次：一次做成 EU 题，一次做成 EA 题。也就是说，第 i 个情境对应的 EU_i 与 EA_i 讲的是同一段情境。

这一点在数据里可以直接验证：`EA_i` 的 `scenario` 字段和 `EU_i` 的 `question` 字段文本完全相同（206 组全部一致），`subject` 也一样。

```
一段情境
   ├── EU  问：当事人可能正在经历什么情绪？
   └── EA  问：应该怎么回应，或者做点什么？
```

这种配对是我们有意设计的，好处是能同时观察同一段人际场景下的两种能力。

**但如果要用它做模型开发，请务必注意：** 划分训练集 / 开发集 / 测试集时，由同一个情境派生出的 EU 题和 EA 题必须落在同一侧。否则模型可能在一道题里见过这段情境，换道题又遇到几乎一样的内容——这就变成了信息泄漏，分数也会随之虚高。

## 题目构成与作答格式

412 道题里，**379 道是单选题**（约 92.0%），**33 道是双选题**（约 8.0%）。

双选题的正确答案是两个选项组成的集合。比如金标准写成 `CD`，意思就是 C 和 D **两个都要选**；顺序无所谓：

```
CD == DC
```

## 文件与字段

```
data/
├── eu.jsonl
└── ea.jsonl
```

每行一个 JSON 对象。EU 的字段是 `qid, language, module, question_type, question, subject, choices, answer`，EA 在此基础上多一个 `scenario`。

**当前发布版本里带着金标准答案。** 这让本地复现评测变得很方便，但同时意味着这个公开基准**不具备抗污染能力**——这一点后面会专门说明。

## 数据是怎么来的

### 情境的来源

这些情境取材于日常生活中的人际关系与情绪场景：家庭与亲情,朋友与同伴,关系恋爱与亲密关系,学习与校园,职场与工作,社会交往与公共场景,成就与失败,道德与责任,失去与变化,压力与不确定性。每一段都是为评测专门构造的，而不是从现成材料里直接搬过来的。

我们没有想要复现或取材于私人对话、可识别的个人记录、专有数据集，或保密的用户信息。所以更准确的说法是：这是一个**构造型基准数据集**，而不是传统意义上那种"自然发生的人类受试者数据"。数据中也不包含真实姓名、联系方式、住址之类的个人信息。

### LLM 在构建过程中扮演了什么角色

构建过程用到了大型语言模型，但它是**辅助工具，不是标注者**。它帮忙做的事情包括：生成候选回应、提出候选答案的措辞、协助打磨表达和情境、探索同一类复杂情绪还能怎么讲。

**LLM 生成的内容不会被自动当成金标准。** 所有候选题目和答案在进入数据集之前，都经过了人工审核与修改。所以发布出来的标签，应该被理解为：

> 经过人工审核、同时受到 LLM 辅助构建过程影响的基准标注，而不是未经核验的模型自动输出。

不过说实话，当前版本对"LLM 具体参与了多少、参与了哪些环节"记录得还不够细。之后的版本应该更系统地交代：用了哪些模型、在哪些阶段用了、候选结果是怎么筛的、生成结果和人工判断冲突时又怎么取舍。

### 人工审核做了什么

人工审核主要盯着几件事：情境本身是否合理、中文表达是否自然、在人际上是否得体、情境和选项是否对得上、预期答案能不能站得住脚。有部分 EA 选项被改写，就是为了更贴近日常中文的说法——所以最终选项不一定总能在抽象层面严格对应某条形式化原则，但只要它在具体情境里是个合理、有用的回应，就会留下来。

### 关于标注一致性，需要坦白

**当前版本没有提供完整的标注者间一致性（inter-annotator agreement）统计。** 人工审核的规模不大，也不是按正式的多标注者标注研究来设计的。这是一个明确的短板，不是可以一带而过的细节。

后续版本可以考虑补上这些：多名独立标注者、清晰的标注指南、一致性统计、分歧分析、仲裁流程，以及用来标记"这题本身有歧义 / 标注者信心不足"的标签。

## 情绪分类体系

EU 模块使用 **16 个较粗粒度的情绪类别**。这套分类部分参考了与 Google 相关的研究和公开资源中的情绪分类材料（具体出处还需要补上一条正式引用）。

这里更想强调的是它的定位：它只是一个**够用的基准标签空间**，而不是一套完整的人类情绪理论。

它既不文化中立，也没有穷尽所有情绪。把一个情绪分类体系直接翻译成中文，未必能完整保留强度、效价、人际含义、情境细微差别，以及中文里那些特有的情绪表达之间的区别——中文中有些说法，这套标签目前是接不住的。构建一套更系统、更贴中文语境的情绪分类体系，是后续版本的一个重点方向。

## 金标准答案到底代表什么

### EU 标签的含义

EU 的金标准是这段情境**预期的主导情绪**。它并不代表：

- 这个情境在心理学上只有一种解释；
- 别的情绪不可能出现；
- 每个标注者都必然会选同一项；
- 它构成任何临床或心理学意义上的判断。

现实中一个人可能同时体会到好几种情绪。只要求选一个主要情绪，是我们为了让模型输出能够被一致评估而做的一种简化。

### EA 标签的含义

EA 的答案是人工审核过的**规范性判断**：在这个情境下，什么样的回应是得体的、有支持性的、对关系有建设性的。

但情绪上棘手的情境，往往容得下不止一个合理回应。所以：

> EA 的金标准不等于"人类唯一能接受的回答"。

用单一金标准，主要是评估上的简化。这意味着模型有时会给出一个同样说得通的方案，却因为没有和金标准完全一致而丢分——这是用精确匹配（Exact Match）去评估开放式社会推理任务时固有的局限。我们选择接受它，并在报告时把它讲清楚。

## 评测是怎么做的

### 单选题

预测和金标准选项完全一致，就算对：

```
Gold: B   Prediction: B   → 正确
```

### 双选题

双选题把预测当作**无序集合**来看：

```
Gold: CD   Prediction: DC   → 正确
```

评测器接受常见的写法差异，比如 `C,D`、`C D`、`C和D`、`C and D`，并且会在打分前先统一规范化。对于无效或者本身有歧义的预测，评测器**不会去猜**，直接记为无效（0 分）。

### 双选题部分得分

除了精确匹配，双选题还可以算一个部分得分，用来做诊断：

```
部分得分 = |预测 ∩ 金标准| / |金标准|
```

比如金标准是 `CD`，模型只答了 `C`，得分就是 1 / 2 = 0.5。

它只是补充信息，不能拿来替代精确匹配准确率。

### 会报告哪些指标

- EU Accuracy / EA Accuracy / Overall Accuracy
- Single-choice Accuracy / Two-choice Accuracy
- EA Single-choice Accuracy / EA Two-choice Accuracy
- Two-choice Partial Score

EU 和 EA 建议分开报——它们测的毕竟不是同一种能力。

## 数据集里的"捷径"：分布伪影与参考基线

这个数据集在答案分布上有几处明显的偏斜，报分时值得留意。

**EU 单选题**里 `C` 占了 89 / 206 = **43.20%**。换句话说，一个"永远选 C"的策略就能拿到大约 43.20% 的 EU 单选准确率。

**EA 单选题**里 `A` 占了 88 / 173 = **50.87%**——"永远选 A"就能到 50.87%。

把 379 道单选题合起来看：

| 选项 | 数量 | 占比 |
| --- | ---: | ---: |
| A | 113 | 29.8% |
| B | 65 | 17.2% |
| C | 127 | 33.5% |
| D | 74 | 19.5% |

**双选题**里 `AB` 出现了 15 / 33 = **45.45%**，所以"永远答 AB"能拿到约 45.45% 的双选精确匹配准确率。

## 选项长度这个伪影更得注意

EA 单选子集里藏着一条相当强的风格信号：**"唯一最长的那个选项"恰好就是金标准答案**，这种情况占到 121 / 173 = **69.94%**。

同样一条启发式，放到 EU 单选题上只有 3 / 206 = **1.46%**。

也就是说，一个模型如果完全不去理解情绪，只是"挑那个写得最长、看起来最有共情味道的选项"，在 EA 上就能拿到接近七成的成绩。这意味着当前 EA 数据里可能存在着一种跟情绪智力无关、却可以被利用的捷径。

### 所以报分时请带上基线

建议在报告里一并给出简单启发式基线的结果：

- 固定选项（always A / always C / always AB）
- 随机选择
- 按答案分布来猜
- "唯一最长选项"

脱离这些基线单独谈高分，是很难解释清楚的。

### 可以参考的基线数值

| 策略 | 指标 | 准确率 |
| --- | --- | ---: |
| 始终预测 `C` | Overall | 30.83% |
| 始终预测 `C` | EU | 43.20% |
| 始终预测 `A` | EA 单选 | 50.87% |
| 始终预测 `AB` | 双选 | 45.45% |
| 选择唯一最长选项 | EA 单选 | 69.94% |
| 选择唯一最长选项 | EU 单选 | 1.46% |

这些数字不是用来当"有竞争力的基线"的，而是为了把数据集自身的伪影摊开在明面上。

## 和 EmoBench 的关系

这个基准在设计上受了 **EmoBench** 的启发，尤其是"把情绪理解与情绪应用分开来测"这个思路。EmoBench 是我们的概念参考。

这里也要说清楚：本数据集**不声称复现或翻译 EmoBench 中的任何题目**。这里的情境、选项和标注都是独立构建的。至于第三方的理论框架和分类体系参考资料，应该和情境本身的来源分开来引用。

## 已知局限

1. **情绪分类偏粗。** 16 个类别覆盖不了中文情绪表达的多样性和细微差别。
2. **情绪状态本身就是模糊的。** 现实情境常常同时存在好几种情绪，而 EU 只要求挑一个主导情绪。
3. **EA 用的是规范性标签。** 一道 EA 题可能在社交层面容得下多个合理答案，但基准只给了一个金标准。
4. **人工标注的规模有限。** 目前没有建立起完整的标注者间一致性证据。
5. **构建过程有 LLM 参与。** 虽然都经过人工审核，但这部分的记录还不够系统。
6. **选项层面存在伪影。** 某些答案分布和风格模式，可能提供与情绪智力无关的捷径。
7. **金标准答案是公开的。** 因此它不适合被当作具有抗污染能力的私有测试集。如果模型或评测系统能拿到已经公开的标签，就可能在没有真正展示目标能力的情况下，拿到人为偏高的分数。

## 建议怎么用

这个数据集适合：中文情绪智力研究、模型之间的比较、提示词与指令效果的评估、情绪理解与情绪应用的分析、对模型行为做诊断，以及为更好的中文社会情绪推理基准打底。

如果用这个基准做研究，建议一并报告这些内容：

1. EU 与 EA 各自的分数；
2. 单选与双选各自的表现；
3. 精确匹配分数与双选部分得分；
4. 简单启发式基线的结果；
5. 模型名称与版本；
6. 推理设置；
7. 模型此前是否接触过这个公开数据集。

## 可复现性

为了结果可复现，建议做到：

- `temperature = 0`；
- 固定模型与版本标识；
- 记录完整的推理参数；
- 保存模型的原始响应；
- 使用仓库里提供的规范化与评测脚本；
- 报告失败或无效预测的数量；
- 记录所使用数据集的确切版本。

带推理（reasoning）的模型，可能比普通对话模型需要更大的 token 预算。评测流程内置了重试与断点续跑，所以单次 API 失败通常不会让整轮评测白跑。

## 污染与基准完整性

这个基准是公开的，而且当前发布的数据里就带着金标准答案，所以它应当被当作一个**开放基准**，而不是"安全的排行榜测试集"。

如果将来真要搭排行榜，比较稳妥的做法是分成两层：

```
公开开发集：标签公开，本地评测
私有测试集：标签保密，服务端评测
```

私有测试集最好能做到：情境是新的、没见过的；金标准不公开；按情境级别做划分隔离（别让同一段情境横跨两侧）；不让人直接提交金标准答案；同时记录模型与提交的元数据。

## 伦理

这个数据集涉及情绪、人际关系和社会行为，用途是研究与评估，**不是**用来做临床诊断、心理健康评估、判断某个人的心理状态，或对个人做高风险决策。

题目里的情境是构造出来的基准示例，不该被当成临床案例。同样地，也不要把模型在这个基准上的表现，说成"模型拥有人类般情绪体验"的证据——它衡量的是**可观察的任务表现**，而不是主观体验。

## 许可证

仓库里的代码按 [LICENSE](LICENSE)（MIT）发布。**数据集许可证需要和代码许可证分开考虑**：数据集许可证由数据集作者另行指定。

在明确的数据集许可证发布之前，请不要默认这个数据集可以自由再分发，也不要默认所有商业用途都被允许。

## 引用

如果你在研究中用到了这个基准，请通过仓库里的 [CITATION.cff](CITATION.cff) 引用。


## 小结

**CEUA Benchmark** 是一个中文情绪智力基准，共 **412 道题**，来自 **206 个基础人际情境**，评估两种相关的能力：

- **EU（情绪理解）**：识别一段情境所暗示的主导情绪；
- **EA（情绪应用）**：选择恰当的人际回应或行动。

它被设计成一个开放的研究基准。当前这一版强调透明和可复现，同时也如实承认自己的局限：情绪类别偏粗、EA 用的是单一规范性金标准、缺少充分的标注一致性证据、选项层面存在伪影、构建过程有 LLM 参与，以及金标准答案是公开的。

所以，解读分数的时候，请把它当作"需要结合数据集特征和参考基线一起看"的结果，而不是一个可以单独引用的绝对数字。
